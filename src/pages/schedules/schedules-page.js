import React from 'react';

import { SchedulesPageContent } from './styled-components';

const SchedulesPage = () => (
  <SchedulesPageContent>Coming soon</SchedulesPageContent>
);

export default React.memo(SchedulesPage);
